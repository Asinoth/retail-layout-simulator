"""TOMACS Tier-1 experiments package.

Runners in this package are headless (no Tk root) and reproducible
(every run writes a JSON sidecar with seed, git SHA, MC iteration count,
elasticity snapshot, and result CSV/PNG paths).
"""
