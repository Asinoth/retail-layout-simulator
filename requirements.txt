# Direct dependencies of the retail-layout simulator and its experiment
# harness, pinned to the versions the experiment artifacts were produced
# with (CPython 3.13.2).
#
#   python -m pip install -r requirements.txt
#
# requirements-lock.txt pins the whole resolved environment -- these
# packages and everything they require -- and is what every experiment
# sidecar checks the running environment against:
#
#   python -m pip install -r requirements-lock.txt

numpy==2.2.4
scipy==1.15.3
pandas==2.2.3
matplotlib==3.10.1

# Excel ingestion for UCI Online Retail II (.xlsx).
openpyxl==3.1.5

# PDF output of make_status_report.py; not needed by the experiments.
reportlab==4.5.1

# Test suite.
pytest==9.0.3
